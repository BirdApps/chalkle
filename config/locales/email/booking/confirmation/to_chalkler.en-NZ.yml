en-NZ:
  email:
    booking:
      confirmation:
        to_non_chalkler:
          subject: "%{name}, you have a new booking for %{course_name} on Chalkle.com"
          body:
            html: |
              %{booker_name} has successfully booked you in to [%{course_name}](%{course_url}). 

              The booking details are as follows:

              [%{booking_name}](%{course_url})

              [%{course_name}](%{course_url})

              %{start_at}
              %{time_range}
              %{address}
              %{venue}

              If you have any questions please contact the provider, [%{provider_name}](%{provider_url}), directly.

              %{paid}

              Happy learning!
            text: |
              %{booker_name} has successfully booked you in to [%{course_name}](%{course_url}). 

              The booking details are as follows:

              %{booking_name}

              %{course_name}

              %{start_at}
              %{time_range}
              %{address}
              %{venue}

              If you have any questions please contact the provider, %{provider_name} directly.

              %{paid}


              Happy learning!

              =======================

              %{course_url}
              %{provider_url}

              =======================
        to_chalkler:
          paid: "You should have also received a payment receipt separately."
          subject: "%{name}, you have a new booking for %{course_name}"
          body:
            html: |
              Your booking is confirmed for [%{course_name}](%{course_url}). 

              The booking details are as follows:

              [%{booking_name}](%{course_url})

              [%{course_name}](%{course_url})

              %{start_at}
              %{time_range}
              %{address}
              %{venue}

              If you have any questions please contact the provider, [%{provider_name}](%{provider_url}), directly.

              %{paid}


              Happy learning!
            text: |
              Your booking is confirmed for %{course_name}. 

              The booking details are as follows:

              %{booking_name}

              %{course_name}

              %{start_at}
              %{time_range}
              %{address}
              %{venue}

              If you have any questions please contact the provider, %{provider_name} directly.

              %{paid}


              Happy learning!

              =======================

              %{course_url}
              %{provider_url}

              =======================