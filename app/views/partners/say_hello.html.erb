<section id="hello" class="grid-container"> 
  <div class="grid-60 prefix-20">
  <p>
    In order to be as helpful as possible, we’d love to learn a little bit more about your organisation. Answer the questions below and we’ll be in touch directly with the next steps.
  </p>
  <p>
  Or if you have a question, grab us directly here: <%= mail_to "hello@chalkle.com", 'hello@chalkle.com' %>.
  </p>

</section>

<section class="grid-container" id="hello-form">

  <div class="grid-60 prefix-20 grid-parent">


    <%= bootstrap_form_for 'said_hello', url: 'said_hello' do |f| %> 
      
      <% if @partner_inquiry.errors.any? %>

        
        <div class="grid-100 errors">
          <h3>There were some errors on your form: </h3>
          <% @partner_inquiry.errors.full_messages.each do |msg| %>
            <div class="error"><%= msg %></div>
          <% end %>
        </div>

      <% end %>

      <div class="grid-100 grid-parent">
        <h1 class="grid-100">Contact Details</h1>

        <div class="form-item">
          <%= f.label :name, "What is your name?*", class: "grid-45" %>
          <%= f.text_field :name, class: "grid-50" %> 
        </div>

        <div class="form-item">
          <%= f.label :organisation, "What is your organisation called?", class: "grid-45" %>
          <%= f.text_field :organisation, class: "grid-50" %>
        </div>

        <div class="form-item">
          <%= f.label :location, "Where are you based?", class: "grid-45" %> 
          <%= f.text_field :location, class: "grid-50" %>
        </div>

        <div class="form-item">
          <%= f.label :contact_details, "What email or phone number is best used to contact you?*", class: "grid-45" %>
          <%= f.text_field :contact_details, class: "grid-50" %>
        </div>

      </div>

      <div class="grid-100">
        <div class="form-item">
          <%= f.label :comment, "What do you want to achieve with Chalkle°?", class: "grid-80 prefix-10 suffix-10" %>
          <%= f.text_area :comment, class: "grid-80 prefix-10 suffix-10", rows: 4  %> 
        </div>
        <div class="submit-wrapper grid-100">
          <%= f.submit "Say Hello", class: 'hello-button'%> 
        </div>
      </div>

    <% end %>
  </div>

</section>