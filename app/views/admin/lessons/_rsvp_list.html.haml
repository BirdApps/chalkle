%table
  %thead
    %tr
      %th Name
      %th Email
      %th Guests
      %th Status
      %th Cost
      %th Paid
      -if role!="super"
        %th Answers
      -if role=="super"
        %th Booking
        %th Email them
      %th May cancel
      %th Cash
  %tbody
    -bookings.each do |b|
      %tr
        %td= link_to b.chalkler.name, admin_chalkler_path(b.chalkler)
        -if b.chalkler.meetup_id.present?
          %td= link_to b.chalkler.meetup_id, "http://www.meetup.com/" + group_url.first + "/message/?recipientId=#{b.chalkler.meetup_id}"
        -elsif b.chalkler.email.present?
          %td= mail_to b.chalkler.email
        -else
          %td= "no contact"
        %td= b.guests
        %td= b.status
        %td= number_to_currency b.cost
        %td= b.paid
        -if role!="super"
          %td
            =render "/admin/bookings/answers", :answers => b.answers if b.answers
        -if role=="super"
          %td= link_to 'View', admin_booking_path(b) if can? :read, b
          -if b.reminder_email
            %td= link_to ( b.status=='no-show' ? 'No-show' : 'Reminder' ), "http://www.meetup.com/" + group_url.first + "/message/?recipientId=#{b.chalkler.meetup_id}&subject=#{b.email_subject}&body=Hi+#{b.chalkler.name.split[0]},#{b.reminder_email}"
          -else
            %td= ""
        -if class_may_cancel
          %td= link_to 'Email them', "http://www.meetup.com/" + group_url.first + "/message/?recipientId=#{b.chalkler.meetup_id}&subject=#{b.email_subject}&body=Hi+#{b.chalkler.name.split[0]},#{may_cancel_email}"
        -else
          %td= ""
        -if (b.cost.present? ? b.cost : 0) > 0 && b.paid != true
          %td= link_to "Pay #{number_to_currency b.cost*1.15}", record_cash_payment_admin_booking_path(b)
