<% if params[:view] == "grid" %>
  <div class="category col-sm-6">
    <%= link_to category.name, v2_category_show_path(category.url_name) %>
  </div>
<% else %>
  <div class="category col-xs-12">
    <%= link_to category.name, v2_category_show_path(category.url_name) %>
  </div>
<% end %>