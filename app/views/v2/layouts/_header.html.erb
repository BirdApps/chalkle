<header class="header" style="background-image: url( <%= @header_bg ? @header_bg : '/assets/header_bg.jpg' %> )">
  <div class="container">
    <% if chalkler_signed_in? %>
      signed in
    <% else %>
      signed out
    <% end %>
  </div>
</header>
