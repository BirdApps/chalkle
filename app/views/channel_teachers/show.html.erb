
<br /> <br />
<% if @channel_teacher.bio.present? %>
  <p class="center light">
     <%= @channel_teacher.bio %>
  </p>
<% else %>
  <p class="center light">
    <%= @channel_teacher.name %> is a teacher for <%= @channel_teacher.channel.name %>
  </p>
<% end %>

<div class="fancy-header">
  <h3>Upcoming Courses</h3>
  <hr />
</div>

<%= render @courses %>
<% unless @courses.present? %>
  <p class="center light">
    There are no upcoming courses for <%= @channel_teacher.name %>
  </p>
<% end %>