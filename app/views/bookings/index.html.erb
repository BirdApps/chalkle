<%= render 'courses/heading' %>

<div class="bookings">
  
  <% if policy(@course).bookings_csv? %> 
    <%= link_to "View Spreadsheet/CSV", csv_provider_course_bookings_path(@course.path_params), class: 'btn btn-info pull-right' %>
  <% end %>

  <br />
  <br />

  <%= render 'bookings_list' %>
</div>