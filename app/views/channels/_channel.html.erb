<% if params[:view] == "grid" %>
  <div class="channel col-sm-6">
    <%= link_to channel.name, channel_path(channel) %>
  </div>
<% else %>
  <div class="channel col-xs-12">
    <%= link_to channel.name, channel_path(channel) %>
  </div>
<% end %>