<p>Hello <%= @resource.name %></p>

<p><%= @resource.invited_by.name %> has invited you to <%= root_url %>, you can accept it through the link below.</p>

<p><%= link_to 'Accept invitation', accept_invitation_url(@resource, :invitation_token => @resource.invitation_token) %></p>

<% if (bookings = Booking.where(pseudo_chalkler_email: @resource.email) ).any?  %> 
  <p>
    You have <%= bookings.count %> bookings waiting for you for the following classes:
    <ul>
      <% bookings.each do |booking| %> 
        <li><%= link_to booking.course.name, course_url(booking.course) %> Invited by: <%= booking.booker.name %></li>
      <% end %>
    </ul>    
  </p>
<% end %> 

<p>If you don't want to accept the invitation, please ignore this email.<br />
Your account won't be created until you access the link above and set your password.</p>
