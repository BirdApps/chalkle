Hello <%= @chalkler.name.split[0] %>, Here is your chalkle <%= @chalkler.email_frequency %> digest for <%= Date.today.strftime("%A %B #{Date.today.day.ordinalize}") %>.

<% if @new_lessons.any? %>
New chalkle classes:
  <% @new_lessons.each do |lesson| %>
    <%= "#{lesson.name}: #{lesson.meetup_data.present? ? lesson.meetup_data['event_url'] : 'http://www.chalkle.com'}" %>
lesson.start_at.strftime("%a %b %d") if lesson.start_at

truncate(lesson.do_during_class, length: 250)
  <% end %>
<% end %>

<% if @open_lessons.any? %>
There is still time to sign up for these classes:
  <% @open_lessons.each do |lesson| %>
    <%= "#{lesson.name}: #{lesson.meetup_data.present? ? lesson.meetup_data['event_url'] : 'http://www.chalkle.com'}" %>
lesson.start_at.strftime("%a %b %d") if lesson.start_at

truncate(lesson.do_during_class, length: 250)
  <% end %>
<% end %>

Thank you for supporting chalkle and we look forward to seeing you soon!
