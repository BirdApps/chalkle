<%#-*- coding: UTF-8 -*-%>
Hello <%= @chalkler.name.split[0] %>,

Here is your chalkle° <%= @chalkler.email_frequency %> digest for <%= Date.current.strftime("%A %B #{Date.current.day.ordinalize}") %>.

<% if @new_courses.any? %>
New chalkles:

  <% @new_courses.each do |course| %>
<%= "#{course.channel.name}: #{course.name} - #{course.start_at.strftime("%l:%M %p %a %B %d") if course.start_at}" %>

<%= truncate(course.do_during_class, length: 250, separator: ' ') %>
Join class: <%= course.meetup_url %>


  <% end %>
<% end %>

<% if @open_courses.any? %>
There is still time to sign up for these chalkles:

  <% @open_courses.each do |course| %>
<%= "#{course.channel.name}: #{course.name} - #{course.start_at.strftime("%l:%M %p %a %B %d") if course.start_at}" %>

<%= truncate(course.do_during_class, length: 250, separator: ' ') %>
Join <%= course.attendance %> other chalklers: <%= course.meetup_url %>


  <% end %>
<% end %>

--

See more chalkles from:
<% @chalkler.channels.collect do |c| %>
<%= c.name %>: <%= "http://#{c.url_name}.chalkle.com" %>
<% end %>

Thank you for supporting chalkle° and we look forward to seeing you soon!
