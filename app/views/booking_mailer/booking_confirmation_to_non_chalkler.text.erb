<%= 
  paid_text = @course.paid? ? t("email.booking.confirmation.to_chalkler.paid") : ""

    note_to_attendees = @course.note_to_attendees ? "Details for attendees:\n\n #{@course.note_to_attendees}" : nil

   t("email.booking.confirmation.to_chalkler.body.text", booking_name: @booking.name, course_name: @course.name, course_url: tiny_course_url(@course), provider_name: @booking.provider.name, provider_url: provider_url(@booking.provider.url_name), venue: @course.venue, address: @course.address, start_at: day_ordinal_month(@course.start_at), note_to_attendees: note_to_attendees, time_range: pretty_time_range(@course.start_at, @course.end_at), paid: paid_text)
%>