.browse_courses
  .page_header_panel
    .container
      %h1.page-title
        = course_index_welcome_message
      = render partial: 'filters'

  .search_panel.full_page
    #courses_index.container
      %ul.simple-nav#browse_type
        %li{class: ('active' if !@filter.view_months?)}
          %a{:href => "#weeks", 'data-toggle' => "tab"}
            %i.fa.fa-list
            Week View
        %li{class: ('active' if @filter.view_months?)}
          %a{:href => "#months", 'data-toggle' => "tab"}
            %i.fa.fa-calendar
            Month View
      .tab-content
        #weeks.tab-pane{class: ('active' if !@filter.view_months?)}
          = render partial: 'weeks_calendar_view', locals: {week_courses: @week_courses}
        #months.tab-pane{class: ('active' if @filter.view_months?)}
          = render partial: 'months_calendar_view', locals: {month_courses: @month_courses}

:coffeescript
  $ ->
    $('.filter_selector').filterSelector('#courses_index')