<%= render 'heading' %>

<div class="fancy-header">
  <h3>Upcoming Classes</h3>
  <hr />
</div>

<%= render @courses %>
<% unless @courses.present? %>
  <p class="center light">
    There are no upcoming classes for <%= @provider_teacher.name %>
  </p>
<% end %>