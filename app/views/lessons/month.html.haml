- if params[:format] == 'js'
  = render partial: 'month_calendar_view', locals: {month: @month, lessons: @lessons}
- else
  .front_end
    #months
      = render partial: 'month_calendar_view', locals: {month: @month, lessons: @lessons}
