- if params[:format] == 'js'
  = render partial: 'month_calendar_view', locals: {month: @month, lessons: @month_lessons}
- else
  .search_panel
    .container
      #months
        = render partial: 'month_calendar_view', locals: {month: @month, lessons: @month_lessons}
