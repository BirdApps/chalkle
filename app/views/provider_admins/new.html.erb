<div class="hidden-xs col-sm-2">
</div>
<div class="new_form_wrapper col-xs-12 col-sm-8">
  <%= bootstrap_form_for(@provider_admin, wrapper: :simple_bootstrap3, html: { method: :post, class: 'form-horizontal', role: 'form', enctype: "multipart/form-data" }) do |f| %>
    <div class="fancy-header">
      <h3>Create Admin</h3>
      <hr />
    </div>
    <%= f.text_field :provider_id, type: :hidden  %>
     <p class="small grey center">
      If the admin is already registered with chalkle,<br /> be sure to use the email they used when signing up.
    </p>
    <div class="form-group email required provider_teacher_email">
      <div class="control-label col-sm-5">
        <label class="email required" for="provider_teacher_email">
          <abbr title="required">*</abbr> 
          New Admin's Email
        </label>
      </div>
      <div class="col-sm-7">
        <%= f.text_field :email, :'data-error-message' => 'You must enter an email', class: 'form-control' %>
      </div>
    </div>
    <label class="exists-success" style="display:none">
      <hr />
      Great, that person is already registered on Chalkle. <br /> We'll add them as a teacher for the provider <%= @provider.name %>
      <hr />
    </label>

    <label class="exists-warning" style="display:none">
      <hr />
      Even though that person isn't on Chalkle yet, they can still be a teacher - you can make classes for them and they'll receive reminders and notices via email.<br /><br />At any point in time they can sign up to Chalkle to take advantage of the website's features.
      <hr />
    </label>
    <br />
    <div class="col-xs-12 center">
      <input class="btn btn-primary" value="Create Admin" type="submit" />  
    </div>
  <% end  %>
</div>
<div class="hidden-xs col-sm-2">
</div>